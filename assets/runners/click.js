/**
 * 
 * cauto_default_click_step
 * @since 1.0.0
 * 
 * 
 */

var cauto_default_click_step = (params) => {
    return [
        {
            status: 'passed',
            message: "Test click"
        }
    ]

}